# LED Blink Demo Configuration
config APP_LEDBLINK
	bool "LED Blink Demo"
	default y
	depends on USERLED
	---help---
		Enable LED blink demo application for STM32F103ZET6.
		Blinks LED1 (PB0) at 500ms interval.
